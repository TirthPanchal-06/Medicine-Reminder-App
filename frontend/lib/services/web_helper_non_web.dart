// Stub implementation of web helpers for mobile/desktop platforms to ensure compile-safety.
void enableWebNotifications(String token) {
  // No-op on mobile/non-web platforms where native notifications are used instead
}
